rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 進捗は本人(匿名UID)のみ読み書き可
    match /chikei_progress/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
